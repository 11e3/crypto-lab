# VBO 자동매매봇 사용 가이드

업비트 VBO 전략 자동매매봇 (다중 계정 지원)

## 📊 전략 성능 (검증 완료)

**백테스트 결과 (2022-2024):**
- CAGR: 51.92%
- Sharpe: 1.92
- MDD: -14.95%
- 승률: 8/8년 수익 (100%)

**전략:**
- 매수: VBO 돌파 + MA5 + BTC MA20 조건
- 매도: MA5 이탈 or BTC MA20 이탈
- 코인: BTC + ETH (포트폴리오)

**매매 방식:**
- **포트폴리오 배분**: BTC:ETH = 50:50 균등 배분
  - 둘 다 신호 발생 시: 잔고를 2등분하여 각각 매수
  - 한 쪽만 신호 발생 시: 전체 잔고로 해당 코인 매수
  - 예: 100만원 → BTC 신호 + ETH 신호 = 각 50만원씩
- 매수: **시장가 주문** - 타겟 가격 근처에서만 매수
  - 조건: 현재가가 타겟 ±1% 이내일 때만 매수 실행
  - 백테스트: 일봉 기준 "당일 고가 >= 타겟" 체크 (당일 중 언제든)
  - 실거래: 실시간으로 "현재가 ≈ 타겟" 체크 (더 정확)
  - 타겟 벗어나면 해당일 매수 포기 (다음날 재평가)
  - 실제 체결 가격/수량을 조회하여 정확하게 추적
- 매도: **시장가 주문** (즉시 체결)
  - MA5 또는 BTC MA20 이탈 시 즉시 매도
  - 실제 체결 가격으로 수익률 계산

## 🚀 빠른 시작

### 1. 설치

```bash
# 의존성 설치
pip install pyupbit pandas

# 또는
pip install -r requirements.txt
```

### 2. 설정

```bash
# .env 파일 생성
cp .env.example .env

# .env 편집 (API 키 입력)
nano .env  # 또는 vim, code 등
```

### 3. API 키 발급

1. [업비트 Open API 관리](https://upbit.com/mypage/open_api_management) 접속
2. "Open API Key 발급" 클릭
3. **권한 설정:**
   - ✅ 자산 조회
   - ✅ 주문 조회
   - ✅ 주문하기 (거래)
   - ✅ 출금하기 (선택)
4. 2단계 인증 완료
5. Access Key와 Secret Key 복사 → `.env`에 붙여넣기

### 4. 텔레그램 알림 설정 (선택)

```bash
# 1. @BotFather에게 /newbot 명령
# 2. 봇 이름 입력
# 3. 받은 토큰을 TELEGRAM_BOT_TOKEN에 입력

# 4. @userinfobot에게 메시지 전송
# 5. 받은 chat_id를 TELEGRAM_CHAT_ID에 입력

# 6. 봇에게 /start 명령 보내기 (중요!)
```

### 5. 실행

```bash
# 봇 시작
python vbo_bot.py

# 백그라운드 실행 (Linux/Mac)
nohup python vbo_bot.py > bot.log 2>&1 &

# 로그 확인
tail -f bot.log
```

## ⚙️ 설정 (.env 파일)

### 필수 설정

```bash
# 계정 1 (필수)
ACCOUNT_1_NAME=Main
ACCOUNT_1_ACCESS_KEY=your_key
ACCOUNT_1_SECRET_KEY=your_secret
```

### 다중 계정 (선택)

```bash
# 계정 2
ACCOUNT_2_NAME=Sub
ACCOUNT_2_ACCESS_KEY=your_key_2
ACCOUNT_2_SECRET_KEY=your_secret_2

# 계정 3, 4, 5... (무제한)
ACCOUNT_3_NAME=Third
ACCOUNT_3_ACCESS_KEY=your_key_3
ACCOUNT_3_SECRET_KEY=your_secret_3
```

### 전략 파라미터 (선택)

```bash
# 트레이딩 코인 (기본: BTC,ETH)
SYMBOLS=BTC,ETH

# VBO 파라미터
MA_SHORT=5          # 단기 이평선 (기본: 5)
BTC_MA=20           # BTC 이평선 (기본: 20)
NOISE_RATIO=0.5     # 변동성 배수 (기본: 0.5)
```

**⚠️ 주의:** 파라미터는 검증된 기본값 사용 권장. 변경 시 백테스트 필수!

## 📱 텔레그램 알림

봇이 다음 이벤트를 텔레그램으로 알림:

- 🤖 봇 시작/종료
- 🟢 매수 주문
- 🔴 매도 주문
- ⚠️ 에러 발생

**알림 예시:**
```
🟢 BUY [Main]
Symbol: BTC
Price: 50,000,000 KRW
Amount: 1,000,000 KRW
```

## 💾 State 파일 (중요!)

봇은 **자신이 산 코인만** 관리합니다.

### Position Tracker

봇이 매수한 코인을 `.bot_positions_계정명.json` 파일에 기록:

```json
{
  "BTC": {
    "quantity": 0.02,
    "entry_price": 50000000,
    "entry_time": "2024-01-15T09:30:15"
  }
}
```

### 동작 방식

1. **봇이 매수** → State 파일에 기록
2. **봇이 매도** → State 파일에서 제거
3. **매도 시그널** → State 파일에 있는 것만 매도

### 기존 보유 코인 처리

**✅ 안전:**
- 봇 시작 전 BTC 보유 → 봇은 무시 (팔지 않음)
- 봇 운영 중 수동으로 ETH 매수 → 봇은 무시

**⚠️ 주의:**
- State 파일 삭제 → 봇이 보유 코인 잊어버림
- State 파일 수정 → 동작 이상 가능

### 재시작 시나리오

**시나리오 1: 정상 재시작**
1. 봇이 BTC 매수 → State에 BTC 기록
2. 봇 중단
3. 봇 재시작 → State 파일 읽음
4. 매도 시그널 → 정상 매도 ✅

**시나리오 2: State 파일 삭제**
1. 봇이 BTC 매수 → State에 BTC 기록
2. 봇 중단
3. **State 파일 삭제** (실수)
4. 봇 재시작 → BTC 보유 모름
5. 매도 시그널 → **매도 안됨** (수동 처리 필요)

**시나리오 3: 수동 매도 후 재시작**
1. 봇이 BTC 매수 → State에 BTC 기록
2. 봇 중단
3. 수동으로 BTC 매도 (업비트 앱)
4. 봇 재시작 → State에 BTC 있음
5. 매도 시도 → 잔고 없음 감지 → State에서 자동 제거 ✅

**시나리오 4: 기존 보유 코인**
1. 수동으로 ETH 보유 중
2. 봇 시작 → State 파일 비어있음
3. ETH 매도 시그널 → **매도 안함** (State에 없음) ✅
4. 기존 ETH는 안전하게 보관됨

### State 파일 백업

중요! State 파일은 정기적으로 백업:

```bash
# 백업
cp .bot_positions_Main.json .bot_positions_Main.json.backup

# 복구 (문제 발생 시)
cp .bot_positions_Main.json.backup .bot_positions_Main.json
```

## 🔍 모니터링

### 로그 확인

```bash
# 실시간 로그
tail -f bot.log

# 최근 50줄
tail -50 bot.log

# 에러만 보기
grep ERROR bot.log
```

### 로그 예시

```
2024-01-15 09:00:01 [INFO] VBO Trading Bot Started
2024-01-15 09:00:01 [INFO] Symbols: BTC, ETH
2024-01-15 09:00:01 [INFO] Accounts: 2
2024-01-15 09:00:15 [INFO] [Main] BUY BTC: 1,000,000 KRW @ 50,000,000
2024-01-15 18:30:42 [INFO] [Sub] SELL ETH: 0.5 coins
```

## 🛡️ 안전 수칙

### 1. API 키 관리
- ✅ `.env` 파일은 절대 공유하지 마세요
- ✅ Git에 커밋하지 마세요 (`.gitignore`에 이미 포함됨)
- ✅ 불필요한 권한은 주지 마세요
- ✅ 정기적으로 키 재발급

### 2. 자금 관리
- ✅ 처음엔 소액으로 테스트
- ✅ 전 재산 투자 금지
- ✅ 손실 감당 가능한 금액만 투자
- ✅ 정기적으로 수익 인출

### 3. 운영 관리
- ✅ 정기적으로 로그 확인
- ✅ 텔레그램 알림 활성화
- ✅ 서버 안정성 확인 (VPS 권장)
- ✅ 봇 업데이트 주기적 확인

### 4. 리스크
- ⚠️ 과거 수익이 미래 수익을 보장하지 않음
- ⚠️ 급격한 시장 변화 시 손실 가능
- ⚠️ API 제한, 거래소 점검 등 기술적 리스크 존재
- ⚠️ 24/7 운영 필요 (서버 다운 시 거래 중단)

## 🔧 트러블슈팅

### Q1: "No accounts configured" 에러

**원인:** `.env` 파일이 없거나 API 키가 잘못됨

**해결:**
```bash
# 1. .env 파일 존재 확인
ls -la .env

# 2. 내용 확인 (키 일부만 보임)
head .env

# 3. 올바른 형식인지 확인
# ACCOUNT_1_NAME=Main
# ACCOUNT_1_ACCESS_KEY=aBcD1234...
# ACCOUNT_1_SECRET_KEY=XyZ9876...
```

### Q2: 매수/매도가 안됨

**원인:**
1. API 권한 부족
2. 잔고 부족
3. 최소 주문 금액 미달 (5,000원)

**해결:**
```bash
# 1. API 권한 확인 (업비트 > Open API 관리)
# "주문하기(거래)" 권한 필요

# 2. 잔고 확인
# 업비트 앱에서 확인

# 3. 로그 확인
grep "Failed\|Error" bot.log
```

### Q3: 텔레그램 알림이 안옴

**원인:**
1. 토큰/Chat ID 오류
2. 봇에게 /start 안보냄

**해결:**
```bash
# 1. 봇에게 직접 /start 명령 보내기

# 2. Chat ID 재확인
# @userinfobot에게 메시지 보내서 확인

# 3. 테스트
python -c "
from urllib.request import Request, urlopen
import urllib.parse
token = 'YOUR_TOKEN'
chat_id = 'YOUR_CHAT_ID'
url = f'https://api.telegram.org/bot{token}/sendMessage'
data = urllib.parse.urlencode({'chat_id': chat_id, 'text': 'Test'}).encode()
urlopen(Request(url, data=data))
print('Success!')
"
```

### Q4: 서버 재시작 시 자동 실행

**Linux (systemd):**

```bash
# 1. 서비스 파일 생성
sudo nano /etc/systemd/system/vbo-bot.service

# 2. 내용 입력
[Unit]
Description=VBO Trading Bot
After=network.target

[Service]
Type=simple
User=your_username
WorkingDirectory=/path/to/0-bot-private
ExecStart=/usr/bin/python3 /path/to/0-bot-private/vbo_bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target

# 3. 활성화
sudo systemctl daemon-reload
sudo systemctl enable vbo-bot
sudo systemctl start vbo-bot

# 4. 상태 확인
sudo systemctl status vbo-bot

# 5. 로그 확인
sudo journalctl -u vbo-bot -f
```

## 📊 성능 모니터링

### 수익률 확인

업비트 웹/앱에서 확인:
- 보유자산 > 거래내역
- 통계 > 일별/월별 수익률

### 전략 검증

정기적으로 백테스트 실행:
```bash
# 최근 성능 확인
python backtest_vbo_portfolio.py --start 2024-01-01

# 전체 기간 확인
python backtest_vbo_portfolio.py
```

### 매매 기록 분석

봇은 모든 거래를 `trades_계정명.csv` 파일에 자동 저장합니다.

**CSV 형식:**
```csv
timestamp,date,action,symbol,price,quantity,amount,profit_pct,profit_krw,strategy,note
2025-01-15T09:30:15,2025-01-15,BUY,BTC,50000000,0.02,1000000,,,VBO,
2025-01-16T18:45:22,2025-01-16,SELL,BTC,52000000,0.02,1040000,4.00,40000,VBO,
```

**분석 도구:**

```bash
# 전체 성능 분석
python analyze_live_trades.py

# 특정 계정만 분석
python analyze_live_trades.py Main

# 특정 기간 분석
python analyze_live_trades.py --since 2025-01-01

# 최근 거래 20개 표시
python analyze_live_trades.py --recent 20
```

**분석 결과:**
- ✅ 총 거래 수, 승률, ROI
- ✅ 코인별 성과 (BTC, ETH 등)
- ✅ 백테스트와 실거래 비교
- ✅ 최근 거래 내역
- ✅ 최고/최악 거래

**백테스트 비교 예시:**
```
BACKTEST COMPARISON (BTC+ETH Strategy)
======================================================================
Metric                   Expected         Actual           Diff
----------------------------------------------------------------------
CAGR                        51.92%         48.35%         -3.57%
Win Rate                   100.00%         87.50%        -12.50%
Sharpe Ratio                  1.92            N/A            N/A
Max Drawdown               -14.95%            N/A            N/A
======================================================================

ASSESSMENT:
✅ Performance is within expected range

NOTE:
- Backtest expectations are based on 2022-2024 test period
- Live trading includes real slippage and execution delays
- Short trading periods may show high variance
- Need at least 20+ trades for statistical significance
```

**중요:**
- 매매 기록은 자동으로 저장됨
- `.gitignore`에 포함되어 깃헙에 업로드 안됨
- 정기적으로 백업 권장
- 최소 20회 이상 거래해야 통계적 의미 있음

## 📞 지원

문제가 있으면:
1. 로그 파일 확인 (`bot.log`)
2. 이 가이드의 트러블슈팅 섹션 확인
3. GitHub Issues에 문의

## 📜 라이선스

이 봇은 연구/교육 목적으로 제공됩니다.
실제 거래에 사용 시 발생하는 손실에 대해 책임지지 않습니다.

**투자 판단과 손익은 전적으로 사용자 책임입니다.**

---

**⚡ Quick Commands**

```bash
# 설치
pip install pyupbit pandas

# 설정
cp .env.example .env
nano .env

# 실행
python vbo_bot.py

# 백그라운드
nohup python vbo_bot.py > bot.log 2>&1 &

# 중지
pkill -f vbo_bot.py

# 로그
tail -f bot.log
```

**Good luck! 🚀**
