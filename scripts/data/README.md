# 데이터 관리 스크립트

이 디렉토리에는 데이터 관리 스크립트가 포함되어 있습니다.

## 참고

데이터 수집을 위해서는 CLI 명령어를 사용하세요:
```bash
upbit-quant collect --tickers KRW-BTC KRW-ETH --intervals day week
```

`data_collector.py` 스크립트는 하위 호환성을 위해 유지되지만 CLI 사용을 권장합니다.
